// import { put, all } from 'redux-saga/effects';

// import wretch from '../../../services/http';

// eslint-disable-next-line require-yield
export function* getDesigns() {
  // eslint-disable-next-line no-alert
  alert(1);
}

export function* handleDesigns() {
  yield getDesigns();
}
